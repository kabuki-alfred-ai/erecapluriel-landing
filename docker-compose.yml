version: '3.8'
services:
  erecapluriel-landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: erecapluriel-landing
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped